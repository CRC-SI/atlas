define([
  'atlas/model/GeoEntity',
  'atlas/lib/utility/Log'
], function (GeoEntity, Log) {
  /**
   * @typedef atlas.model.LineNetwork
   * @ignore
   */
  var LineNetwork;

  LineNetwork = GeoEntity.extend({

  });

  return LineNetwork;
});
